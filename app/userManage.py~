#!/usr/bin/env python
# encoding: utf-8
from app import db
from app import User

#添加用户信息
def add_user(userinfo):
    user = User()
    getid = False

    #获取用户id
    if 'id' in userinfo:
        #如果获取到用户id,则为老用户修改信息
        user = User.query.filter_by(user_id=userinfo['id']).first()

